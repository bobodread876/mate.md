# Memory Adapters

MATE.md does not require a memory system.

It may reference:

- Markdown logs
- JSONL logs
- Git commits
- Nostr events
- IPFS CIDs
- SQLite databases
- encrypted files
- vector-store records
- external private systems

The core document only needs a URI, type, and optional hash.
